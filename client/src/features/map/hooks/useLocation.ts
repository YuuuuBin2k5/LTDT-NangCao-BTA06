/**
 * Re-export useLocation from LocationContext for map feature
 * This provides a feature-specific entry point while maintaining separation
 */
export { useLocation } from '../../../store/contexts/LocationContext';
